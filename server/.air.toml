root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/superbadger ./cmd/superbadger"
  bin = "./tmp/superbadger"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor"]
  delay = 200

[log]
  time = false

[misc]
  clean_on_exit = true
